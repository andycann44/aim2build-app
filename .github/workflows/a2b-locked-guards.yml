name: Locked Rules Guard

on:
  pull_request:
  push:
    branches: [ "**" ]

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run locked rules guard
        run: bash scripts/a2b_guard_locked_rules.sh
